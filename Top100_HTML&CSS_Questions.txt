### **HTML Interview Questions (50 Questions)**

#### **Basic HTML Questions**
1. What is HTML, and what does it stand for?  
2. What is the difference between HTML and HTML5?  
3. What are HTML tags, and how do they work?  
4. What is the purpose of the `<!DOCTYPE html>` declaration?  
5. Explain the structure of an HTML document.  
6. What are semantic HTML elements, and why are they important?  
7. What is the difference between `<div>` and `<section>` tags?  
8. How do you create a hyperlink in HTML?  
9. What is the purpose of the `<meta>` tag in HTML?  
10. What are HTML attributes, and how are they used?  
11. What is the difference between `<b>` and `<strong>` tags?  
12. How do you add an image to a webpage using HTML?  
13. What is the purpose of the `alt` attribute in the `<img>` tag?  
14. What are self-closing tags in HTML? Give examples.  
15. How do you create a list in HTML? Explain ordered, unordered, and definition lists.  
16. What is the difference between `<br>` and `<hr>` tags?  
17. How do you create a table in HTML?  
18. What is the purpose of the `<head>` tag in HTML?  
19. What is the role of the `<title>` tag?  
20. How do you add comments in HTML?  

#### **Intermediate HTML Questions**
21. What are HTML forms, and how do you create one?  
22. Explain the difference between `GET` and `POST` methods in HTML forms.  
23. What are the new input types introduced in HTML5?  
24. How does the `<canvas>` element work in HTML5?  
25. What is the purpose of the `<figure>` and `<figcaption>` tags?  
26. Explain the role of the `<iframe>` tag.  
27. What is the difference between `<progress>` and `<meter>` tags?  
28. How do you make a website accessible using HTML?  
29. What is the purpose of the `data-*` attribute in HTML5?  
30. How do you embed a video in HTML5?  
31. What is the Geolocation API in HTML5, and how is it used?  
32. What is the purpose of the `<nav>` tag?  
33. How do you create a responsive image using the `srcset` attribute?  
34. What is the difference between block-level and inline elements?  
35. How do you create a dropdown menu in HTML?  
36. What is the `<article>` tag used for?  
37. How do you use the `<details>` and `<summary>` tags?  
38. What is the purpose of the `lang` attribute in the `<html>` tag?  
39. How do you optimize HTML for SEO?  
40. What is the difference between `<script>` and `<noscript>` tags?  

#### **Advanced HTML Questions**
41. How do you ensure cross-browser compatibility in HTML?  
42. What are Web Components, and how do they work with HTML?  
43. Explain the Shadow DOM and its use in HTML.  
44. How do you optimize images in HTML for faster load times?  
45. What is the role of the `manifest` file in HTML5?  
46. How do you handle form validation in HTML5?  
47. What are microdata and schema.org in HTML?  
48. How do you implement lazy loading for images in HTML?  
49. What is the difference between `<em>` and `<i>` tags?  
50. How do you create a custom HTML element?  

### **CSS Interview Questions (50 Questions)**

#### **Basic CSS Questions**
51. What is CSS, and what does it stand for?  
52. What are the different ways to apply CSS to a webpage?  
53. What is the difference between inline, internal, and external CSS?  
54. How do you add comments in CSS?  
55. What is the CSS box model, and what are its components?  
56. What is the difference between `margin` and `padding`?  
57. What are CSS selectors, and what are the different types?  
58. What is the difference between `class` and `id` selectors?  
59. How do you center a div horizontally in CSS?  
60. What is the purpose of the `display` property in CSS?  
61. What is the difference between `display: none` and `visibility: hidden`?  
62. How do you make an element take the full width of the page?  
63. What is the `float` property in CSS, and how does it work?  
64. What are pseudo-classes in CSS? Give examples.  
65. What are pseudo-elements in CSS? Give examples.  
66. How do you create a hover effect in CSS?  
67. What is the `z-index` property in CSS?  
68. What is the difference between `relative`, `absolute`, and `fixed` positioning?  
69. How do you style a button in CSS?  
70. What is the purpose of the `box-sizing` property?  

#### **Intermediate CSS Questions**
71. What is the CSS Flexbox model, and how does it work?  
72. What are the advantages of using CSS Grid over Flexbox?  
73. How do you create a responsive layout using CSS Grid?  
74. What is the `overflow` property in CSS, and how does it work?  
75. How do you create a CSS animation using `@keyframes`?  
76. What is the difference between `transition` and `animation` in CSS?  
77. How do you make images responsive in CSS?  
78. What are media queries, and how are they used for responsive design?  
79. What is the purpose of the `calc()` function in CSS?  
80. How do you use the `:nth-child` pseudo-class?  
81. What is the difference between `rem` and `em` units in CSS?  
82. How do you create a sticky header in CSS?  
83. What is the `clip-path` property in CSS?  
84. How do you implement a CSS reset or normalize?  
85. What is the role of the `::before` and `::after` pseudo-elements?  
86. How do you create a dropdown menu using CSS?  
87. What is the `opacity` property, and how does it affect an element?  
88. How do you handle browser compatibility issues in CSS?  
89. What is the `currentColor` keyword in CSS?  
90. How do you use CSS variables (custom properties)?  

#### **Advanced CSS Questions**
91. What are CSS preprocessors, and what are their benefits?  
92. How do you optimize CSS for performance?  
93. What is the `will-change` property in CSS, and when should you use it?  
94. How do you create a parallax scrolling effect using CSS?  
95. What is the difference between `vw`, `vh`, `vmin`, and `vmax` units?  
96. How do you implement a dark mode toggle using CSS?  
97. What is the `@supports` rule in CSS, and how is it used?  
98. How do you create a responsive navigation menu using CSS?  
99. What is the CSS `content` property, and how is it used with pseudo-elements?  
100. How do you use CSS to achieve automatic numbering for headings and subheadings?  
